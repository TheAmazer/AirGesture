# AirGesture
AirGesture is a state-machine driven HCI tool using MediaPipe and Python for touchless system control. It features precision volume adjustment via index rotation and kinematic swipe detection for navigation. Includes a real-time debug overlay and robust filtering for low-latency performance.
